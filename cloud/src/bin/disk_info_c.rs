// use std::fs;
// // use std::os::windows::fs::MetadataExt;
// use std::path::Path;
// // use std::os::unix::fs::MetadataExt;
// use std::os::macos::fs::MetadataExt;
//
fn main() {
    //     let path = Path::new("C:\\Users\\username\\Desktop\\test");
    //     let volume_path = path.parent().unwrap_or(path);
    //
    //     let metadata = match fs::metadata(volume_path) {
    //         Ok(metadata) => metadata,
    //         Err(err) => panic!("Error getting metadata: {}", err),
    //     };
    //
    //
    //     // let free_space = metadata.free_space().unwrap_or(0);
    //     // println!("Free space: {} bytes", free_space);
}
